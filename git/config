[user]
  name = divyansh
  email = dpi0.dev@proton.me
  signingkey = 5121E1EA634B0201 # GPG Signing Key

[commit]
  gpgSign = true # force all commits to be signed
  verbose = true # show detailed info with diff in the editor

[init]
  defaultBranch = main

[branch]
  sort = -committerdate # sort branches by commit date

[pull]
  default = current # matches upstream branch name when pulling
  rebase = true # When pulling, replay local commits on top of fetched commits instead of creating a merge commit

[push]
  default = simple # matches upstream branch name when pushing
  followTags = true # push release tags automatically

[log]
  date = local # git log date format, Sun Jan 5 14:59:12 2025

[help]
  autoCorrect = prompt # suggests correct command upon mistype

[tag]
  sort = version:refname # sort tags when listed using version aware order system

[credential "https://github.com"]
  helper =
  helper = !/usr/bin/gh auth git-credential

[credential "https://gist.github.com"]
  helper =
  helper = !/usr/bin/gh auth git-credential

# Delta Configuration (https://dandavison.github.io/delta/configuration.html)

[core]
  pager = delta
  editor = nvim
  autocrlf = false
  preloadindex = true # preloads the index into memory for improved performance when running Git commands like status and diff

[merge]
  conflictstyle = zdiff3 # https://www.ductile.systems/zdiff3/
  summary = true # display a brief summary describing what was merged

[interactive]
  diffFilter = delta --color-only

[delta]
  features = decorations
  line-numbers = true
  side-by-side = true
  true-color = always
  hyperlinks = true
  navigate = true  # use n and N to move between diff sections

[delta "interactive"]
  keep-plus-minus-markers = false

[delta "decorations"]
  commit-decoration-style = blue ol
  commit-style = raw
  file-style = omit
  hunk-header-decoration-style = blue box
  hunk-header-file-style = red
  hunk-header-line-number-style = "#067a00"
  hunk-header-style = file line-number syntax

[alias]
  a = add
  aa = add -A
  s = show
  p = push
  pf = push --force
  st = status -sb
  ci = commit
  co = checkout
  b = branch
  l = -c color.diff=true log
  lg = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%aD%C(reset) %C(bold green)(%ar)%C(reset)%C(bold yellow)%d%C(reset)%n'' %C(white)%s%C(reset) %C(dim white)- %an%C(reset)' --all
  lga = -c color.diff=true log --oneline --decorate --graph --all
  lgb = -c color.diff=true log --oneline --decorate --graph --branches
  lo = -c color.diff=true log --oneline --decorate
  ff = merge --ff-only
  last = log -1 HEAD
  unstage = reset HEAD --
  amend = commit --amend --no-edit
  hist = log --pretty=format:'%C(yellow)%h%Creset %ad | %s%d [%an]' --graph --date=short
  fixup = commit --fixup
  squash = rebase -i --autosquash
  undo = reset --hard @{1}
